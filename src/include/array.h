#include<version.h>
importlib<array>

nativefunc length(Array) from array
nativefunc new(int) from array
nativefunc set(Array, int, Object) from array
nativefunc get(Array, int) from array
nativefunc tostr(Array) from array
